<!-- DTD enforcing example structure for prompt optimization workflows -->
  <!-- All description attributes are optional across every example variant. -->
  <!-- Documents may set their DOCTYPE root to either <examples> or a single <example>. -->
  <!ELEMENT examples (good-example | bad-example | example)+>
  <!ATTLIST examples
    <!-- description: Optional summary for the examples collection -->
    description CDATA #IMPLIED>

  <!ELEMENT good-example (#PCDATA | thinking | tooling)*>
  <!ATTLIST good-example
    <!-- description: Optional summary describing the example intent -->
    description CDATA #IMPLIED
    <!-- user-input: Optional sample user instruction associated with the example -->
    user-input CDATA #IMPLIED>

  <!ELEMENT bad-example (#PCDATA | thinking | tooling)*>
  <!ATTLIST bad-example
    <!-- description: Optional summary describing the example intent -->
    description CDATA #IMPLIED
    <!-- user-input: Optional sample user instruction associated with the example -->
    user-input CDATA #IMPLIED>

  <!ELEMENT example (#PCDATA | thinking | tooling)*>
  <!ATTLIST example
    <!-- description: Optional summary describing the example intent -->
    description CDATA #IMPLIED
    <!-- user-input: Optional sample user instruction associated with the example -->
    user-input CDATA #IMPLIED>

  <!ELEMENT thinking (#PCDATA)>

  <!ELEMENT agent (#PCDATA)>
  <!ATTLIST tooling
    <!-- name: Required command or tooling identifier (e.g., Read, Write, Search, or others) -->
    name CDATA #REQUIRED

  <!ELEMENT tooling EMPTY>
  <!ATTLIST tooling
    <!-- name: Required command or tooling identifier (e.g., Read, Write, Search, or others) -->
    name CDATA #REQUIRED
    <!-- params:path: Optional filesystem path parameter -->
    params:path CDATA #IMPLIED
    <!-- params:command: Optional command string parameter -->
    params:command CDATA #IMPLIED
    <!-- params:pattern: Optional search pattern parameter -->
    params:pattern CDATA #IMPLIED>
